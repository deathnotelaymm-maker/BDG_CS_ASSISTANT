Luke v1.17.2-r2 — Integration Origin Contract Hotfix

Use this hotfix only after v1.17.2. It repairs the GitHub PostgreSQL integration fixture so the shared Admin test uses Luke's real shared Admin origin instead of a synthetic hostname that is correctly interpreted as a customer custom domain.

Runtime: 1.17.2
Migration: 045 unchanged
Next migration: 046

Run START-HERE-WINDOWS.bat from the repair package.
