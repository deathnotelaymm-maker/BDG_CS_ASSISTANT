# Legacy Cloudflare Worker — v0.6.8 rollback only

The active v0.7.0 API is `../backend-api/` on Render. Keep this Worker available temporarily for traffic rollback, but do not add new v0.7.0 features here.
