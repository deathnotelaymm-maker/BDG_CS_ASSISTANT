BDG v1.17.0-r2 — WINDOWS REGRESSION PATH HOTFIX

This package repairs the Windows-only `D:\C:\...` path produced during step 6 of the original v1.17.0 installer.

The original application payload was already copied and verified. Do not restore it. Extract this package and run START-HERE-WINDOWS.bat.

This package does not change the application version, migration 043, APIs, database schema, or production behavior.
